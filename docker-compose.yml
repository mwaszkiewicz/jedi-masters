version: '3'

services:

    postgresql:
        image: postgres:9.6.2-alpine
        ports:
          - 5432:5432
    rabbitmq:
        image: rabbitmq:3-management
        ports:
          - 15672:15672
          - 15671:15671
          - 5672:5672